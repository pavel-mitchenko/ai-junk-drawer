# ChatLab

Turn Telegram chat exports into activity stats and charts — message volume over time, top posters, busiest hours.

A small .NET CLI in `src/ChatLab.Cli/` reshapes raw exports into the format the viewer expects.
