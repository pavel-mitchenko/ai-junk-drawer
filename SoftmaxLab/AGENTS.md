# SoftmaxLab

See `README.md` — it covers what the toy does, the math, and the design choices to leave alone.

Single-file vanilla HTML/CSS/JS in `index.html`. Chart.js 4 via CDN. No build, no server.

`alt-styles/` holds visual variants of the same app; `0.html` mirrors `index.html`.
